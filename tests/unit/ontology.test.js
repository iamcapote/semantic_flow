import * as ontology from '../../src/lib/ontology.js';

describe('Ontology', () => {
  it('should define semantic node types', () => {
    expect(ontology).toBeDefined();
    // Check for at least one node type
    // ...
  });
});
