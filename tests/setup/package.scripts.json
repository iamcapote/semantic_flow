{
  "scripts": {
    "test": "jest --setupFilesAfterEnv ./tests/setup/jest.setup.js --coverage",
    "test:unit": "jest tests/unit --setupFilesAfterEnv ./tests/setup/jest.setup.js",
    "test:integration": "jest tests/integration --setupFilesAfterEnv ./tests/setup/jest.setup.js",
    "test:e2e": "playwright test tests/e2e",
    "test:all": "npm run test && npm run test:e2e",
    "coverage": "jest --coverage"
  }
}
